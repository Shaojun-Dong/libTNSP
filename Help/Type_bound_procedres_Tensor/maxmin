maxmin, imaxmin, smaxmin, dmaxmin, cmaxmin, zmaxmin
    There function can output the max or min element of a Tensor. The same as max(char)
    
SYNOPSIS   
    
    type(Tensor)      function     maxmin(char)
    integer           function     imaxmin(char)
    real(kin=4)       function     smaxmin(char)
    real(kin=8)       function     dmaxmin(char)
    complex(kind=4)   function     cmaxmin(char)
    complex(kind=8)   function     zmaxmin(char)
    
    character(len=4)              char
    
    
    
PURPOSE

   here function can output the max or min element of a Tensor. 
   The same as max(char), where char can be:
	   'maxa': max abs (if output a Tensor,real*4,real*8 or integer)
		'mina': min abs (if output a Tensor,real*4,real*8 or integer)
		'maxr': max real
		'minr': min real
		'maxi': 0(not complex data) or max imag
		'mini': 0(not complex data) or min imag
	    

PARAMETERS
 
   char     - character(len=4)  -
            'maxa': max or max abs (complex data)
				'mina': min or min abs (complex data)
				'maxr': max real
				'minr': min real
				'maxi': 0(not complex data) or max imag
				'mini': 0(not complex data) or min imag

EXAMPLT
